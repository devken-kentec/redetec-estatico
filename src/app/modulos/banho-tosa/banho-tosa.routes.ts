import { Routes } from '@angular/router';

// export const homeHoutes: Routes = [
//   { path: '', title: 'Home', loadComponent: ()=> import('./home.component').then((p)=> p.HomeComponent)} //Novo padrão
// ];
